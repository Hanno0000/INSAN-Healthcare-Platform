# Generated by `prisma migrate diff` for test-database.mjs. Never committed:
# a stale copy would silently test a schema that no longer exists.
scripts/full.sql

# Installed on demand: npm i --no-save @electric-sql/pglite
scripts/node_modules/
